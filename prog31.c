#include<stdio.h>
int main()
{
    // if the three sides of triangle are entered through the keyboard, write a program to check the 
    // triangle is isosceles,equilateral, scalene or right angled triangle;

    int a,b,c, one,all,any,sides;

    printf("Enter a: ");
    scanf("%d", &a);
    printf("Enter b: ");
    scanf("%d", &b);
    printf("Enter c: ");
    scanf("%d", &c);

    a,b,c = sides of triangle;

    if(a == b && a == c )
    {
        printf("isosceles triangle");
    }
    else if(a == b && a == c)
    {
        printf("equilateral triangle");
    }
    else if(all sides are diffrent)
    {
        printf("scalene triangle");
    }
    else if(one side=90)
    {
        printf("Right angle triangle");
    }
    return 0;


}
    

    
