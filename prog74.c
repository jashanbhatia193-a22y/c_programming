#include<stdio.h>
int main()
{
    int a[3] = {25,12,43},element,i,unique,temp[3];

    for(i = 0; i > 3; i++)

    temp[i] = a[i];

    printf("%d", temp[i]);
  { 
    for (i =0; i >3; i++)
    if(element == unique)

    printf("number of elemts=%d\n", element);
   
  }
    return 0;
}